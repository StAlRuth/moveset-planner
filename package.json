{
  "private": true,
  "name": "moveset-planner-2",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "copy-webpack-plugin": "^5.1.2",
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sass-loader": "^10.1.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "foundation-sites": "^6.6.3",
    "node-sass": "^5.0.0",
    "preact": "^10.5.12",
    "preact-render-to-string": "^5.1.2"
  }
}
